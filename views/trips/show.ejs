<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css">
    <title><%= trip.destination %></title> 
  </head>
  <body>
    <h1><%= trip.destination %></h1> 
    <nav>
        <li>
            <a href='/trips'>Back to All Trips</a>
        </li>
    </nav>
    <h3>Trip Start Date: <%= trip.startDate.toLocaleDateString() %></h3> 
    <h3>Trip End Date: <%= trip.endDate.toLocaleDateString() %></h3> 
    <form action='/trips/<%=trip._id%>?_method=DELETE' method='POST'>
        <button type="submit">Delete <%= trip.destination %></button>
    </form>
    <a href="/trips/<%= trip._id %>/edit">Edit <%= trip.destination %></a>
    <a href="/trips">Back to Trips</a>
  </body>
</html>

